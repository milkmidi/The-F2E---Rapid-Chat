.sprite{
  display: inline-block;
  background-image: embedurl('img/{{{spritesheet.escaped_image}}}');
  background-repeat: no-repeat;
}

{{#each sprites}}

.{{name}} {
  background-position: {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}}; }
  
{{/each}}