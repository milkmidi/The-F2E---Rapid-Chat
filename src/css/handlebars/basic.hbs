{{#each sprites}}
{{#if @first}}
.sprite {
  display: inline-block;
  background-image: embedurl('img/{{{escaped_image}}}');
  background-repeat: no-repeat; }
{{/if}}

.{{name}} {
  background-position: {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}}; }
{{/each}}